---
title: "STA 313: Advanced Data Visualization"
subtitle: "Dr. Mine Çetinkaya-Rundel"
execute:
  freeze: false
editor_options: 
  chunk_output_type: console
---

This page contains an outline of the topics, content, and assignments for the semester. Note that this schedule will be updated as the semester progresses and the timeline of topics and assignments might be updated throughout the semester.

```{r}
#| echo: false
#| message: false
#| warning: false

library(googlesheets4)
library(gt)
library(tidyverse)
gs4_deauth()
d <- read_sheet("https://docs.google.com/spreadsheets/d/1Qj2rcIqFGwOosr9Yiyo-MaeFSZ7KOYNn8Ph1kebgDuY/edit?usp=sharing")
```

<div>

```{r}
#| echo: false

d |>
  mutate(
    date = as.Date(date)
  ) |>
  gt() |>
  fmt_date(date, date_style = 31) |>
  sub_missing(columns = c(week, topic, prepare, due), missing_text = "") |>
  cols_align(
    align = "center", 
    columns = c(week)
    ) |>
  cols_align(
    align = "right", 
    columns = c(date)
    ) |>
  cols_align(
    align = "left", 
    columns = c(what, topic, prepare, due)
    ) |>
  tab_style(
    style = cell_borders(
      sides = "right",
      color = "#D3D3D3",
      style = "solid"
    ),
    locations = cells_body(
      columns = c(date, topic, prepare)
    )
  ) |>
  fmt_markdown(
    columns = c(topic, prepare, due)
  ) |>
  cols_width(
    week ~ px(40),
    date ~ px(100), 
    due ~ px(100),
    topic ~ px(300)
  ) |>
  cols_label(
    what = ""
  )
```

</div>
